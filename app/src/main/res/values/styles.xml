<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="OctodroidTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabBackground">?attr/selectableItemBackground</item>
        <item name="tabIndicatorColor">@color/black</item>
        <item name="tabIndicatorHeight">4dp</item>
        <item name="tabMaxWidth">@dimen/tab_max_width</item>
        <item name="tabMode">scrollable</item>
        <item name="tabPaddingStart">6dp</item>
        <item name="tabPaddingEnd">6dp</item>
        <item name="tabSelectedTextColor">@color/text_color_primary</item>
        <item name="tabTextAppearance">@style/OctodroidTabTextAppearance</item>
        <item name="tabTextColor">@color/text_color_tertiary</item>
    </style>

    <style name="OctodroidTabTextAppearance" parent="TextAppearance.Design.Tab">
        <item name="android:textSize">@dimen/abc_text_size_button_material</item>
        <item name="android:textColor">@color/text_color_tertiary</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>
